export const environment = {
  production: true,
  apiUrl: 'https://api.example.com', // Esempio di URL per la produzione
  // Aggiungi qui altre chiavi di configurazione per l'ambiente di produzione
};